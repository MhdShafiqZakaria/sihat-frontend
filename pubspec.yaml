name: flutter_test_sihat
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  http: ^1.6.0
  json_annotation: ^4.12.0
  provider: ^6.1.5+1
  ticketcher: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  json_serializable: ^6.14.0
  build_runner: ^2.15.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
